package tn.tekup.auth.controller.api;

import java.util.List;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.web.bind.annotation.DeleteMapping;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.PostMapping;
import org.springframework.web.bind.annotation.RequestBody;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

import tn.tekup.auth.model.Salle;
import tn.tekup.auth.service.SalleService;

//@RestController
//@RequestMapping("/salle")
public class SalleController {

	/*
	 * @Autowired private SalleService salleService;
	 * 
	 * @GetMapping public List<Salle> getAllSalles(){ return
	 * salleService.getAllSalles(); }
	 * 
	 * @PostMapping public Salle saveSalle(@RequestBody Salle salle) { return
	 * salleService.saveOrUpdateSalle(salle); }
	 * 
	 * @DeleteMapping("/{id}") public void deleteSalle(@PathVariable Long id) {
	 * salleService.deleteSalle(id); }
	 */
}
